{
  "name": "@fearless/aot-build",
  "version": "0.1.0",
  "description": "Orchestration: scans a Fearless app's TS source, runs the AOT analyzer + transpiler over each registered handler, emits Rust source for the rust-core to compile in.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\" && tsc",
    "test": "node --import tsx --test test/**/*.test.ts"
  },
  "dependencies": {
    "@fearless/aot-analyzer": "0.1.0",
    "@fearless/aot-transpiler": "0.1.0",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  }
}
